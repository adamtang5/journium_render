import TopNav from "../TopNav";
import HeaderBanner from '../HeaderBanner';

const SplashHeader = () => {
    return (
        <header>
            <TopNav />
            <HeaderBanner />
        </header>
    )
};

export default SplashHeader;
